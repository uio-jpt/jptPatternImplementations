Some interfaces from the AJ version has been transformed into classes for the PT version, like VisitableNode, Node, and Leaf. The reason
for this is to be able to include default implementations of methods. Here, PT would benefit from default implementations of interfaces,
something that is scheduled for Java 8.